# Public URL of resume.json in the S3 content bucket: `terraform -chdir=../infra output content_url`
# Copy to .env.production (safe to commit — it's a public URL, not a secret) so the EC2 build picks it up.
# Leave unset to serve only the content bundled from src/data/resume.json.
VITE_CONTENT_URL=
